/*import {useEffect, useState} from 'react';
import {StockBase} from './StockBaseRow';
import WaitingDisplay from '../WaitingDisplay';


type StockQuote = {
    // The unique identifier of a stock
    symbol : string,
    // The stock name
    stockName : string,
    // The stock type (e.g equity)
    type : string,
    // The currency that the stock is traded in
    stockCurrency : string,
    // Object create timestamp
    lastUpdated : string,
    open : string,
    high : string,
    low : string,
    price : string,
    volume : number,
    latestTradingDay : string,
    previousClose : string,
    change : string,
    changePercent : string

}

type StockQuoteProps = {
    EmptyQuote : JSX.Element,
    QuoteDisplay : JSX.Element,
    QuoteWaiting : JSX.Element,
    UpdateQuote : (stock : StockBase) => Promise<any>
}

function StockQuote(props : StockQuoteProps) : JSX.Element {
    return(

    );
}
export {StockQuote};
export default StockQuote;*/